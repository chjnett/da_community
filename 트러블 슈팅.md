# 🛠️ 다걸고(Dageolgo) 트러블슈팅 가이드

본 문서는 다걸고 플랫폼 개발 및 운영 과정에서 발생한 주요 문제와 해결 방법을 기록합니다.

---

## 1. 인증 및 보안 (Authentication & Security)

### 🚨 문제: 데이터베이스 초기화 후 401/403 에러 발생
*   **증상**: `rm dev.db` 이후 다시 서버를 켰을 때, 로그인을 해도 `401 Unauthorized` 또는 `403 Forbidden`이 뜨며 튕김.
*   **원인**: 브라우저의 로컬 스토리지/쿠키에 남아있는 '이전 DB 시절의 토큰'을 서버가 인식하지 못함.
*   **해결 방법**:
    1. 브라우저 개발자 도구(F12) -> Application -> Local Storage에서 모든 항목 삭제.
    2. 서버 재시작 후 시드 관리자 계정(`20230001@kyonggi.ac.kr`)으로 다시 로그인.

### 🚨 문제: 로그인 후 일부 API 호출 시 튕김 현상 (AUTH_REQUIRED)
*   **증상**: 로그인은 성공했으나 프로필 리포트 등을 불러올 때 갑자기 로그아웃됨.
*   **원인**: 백엔드가 `Authorization` 헤더 방식만 지원하고 쿠키 인증을 처리하지 못해 발생.
*   **해결 방법**: `server.mjs`의 `readAuthUser` 함수를 수정하여 헤더와 쿠키(`accessToken`) 양쪽에서 토큰을 추출하도록 개선.

---

## 2. 관리자 기능 (Admin Dashboard)

### 🚨 문제: AdminUser 타입 참조 SyntaxError
*   **증상**: `Importing binding name 'AdminUser' is not found` 에러 발생.
*   **원인**: Vite 환경에서 인터페이스(Type)를 가져올 때 `import type` 구문을 사용하지 않아 모듈 바인딩 오류 발생.
*   **해결 방법**: `import type { AdminUser } from ...` 형식으로 수정하여 타입 전용 임포트임을 명시.

### 🚨 문제: TanStack Query "No queryFn was passed"
*   **증상**: 관리자 페이지 진입 시 하얀 화면과 함께 쿼리 함수 누락 에러 발생.
*   **원인**: `AdminDashboardPage`에서 사용하는 `aiApi.getLogs` 등이 API 클라이언트에 정의되지 않음.
*   **해결 방법**: `aiApi.ts`에 `getLogs` 메서드를 추가하고 백엔드에 `/api/v1/admin/ai-logs` 엔드포인트 구현.

---

## 3. 라우팅 및 데이터 (Routing & Data)

### 🚨 문제: 게시글 상세 페이지 404 에러
*   **증상**: 피드에서 게시글을 클릭했는데 "해당 데이터를 찾을 수 없음" 메시지 출력.
*   **원인**: 프론트엔드는 `p_1` 형식을 기대하는데 백엔드 시물레이션 데이터는 `post_seed_1` 형식을 사용함.
*   **해결 방법**: `server.mjs`의 `seedIfEmpty` 함수에서 시드 데이터 ID를 `p_1`, `p_2` 형식으로 통일.

### 🚨 문제: 슬래시(/) 유무에 따른 경로 매칭 실패
*   **증상**: `/api/v1/stats/me`는 성공하는데 `/api/v1/stats/me/`는 실패함.
*   **원인**: 백엔드 라우팅 로직이 문자열 완전 일치(`===`) 방식으로 되어 있음.
*   **해결 방법**: `pathname.startsWith()` 또는 정규식을 사용하여 경로 끝의 슬래시 유무와 상관없이 매칭되도록 유연화.

---

## 4. 배포/마이그레이션 (Deploy & Migration)

### 🚨 문제: 알림 페이지 진입 시 `/api/v1/notifications` 500 에러
*   **증상**: 로그인 상태에서 알림 페이지 진입 시 `GET /api/v1/notifications`가 반복적으로 `500` 응답.
*   **원인**:
    1. D1 어댑터(`apps/worker-api/src/db/d1Adapter.mjs`)에 `listNotifications`, `insertNotification`, `markNotificationAsRead` 메서드가 누락됨.
    2. 배포 D1에 `notifications` 테이블이 없어 런타임과 스키마가 불일치함.
*   **해결 방법**:
    1. D1 어댑터에 알림 관련 메서드를 구현.
    2. `apps/worker-api/migrations/0002_add_notifications.sql` 추가 후 원격 적용:
       `wrangler d1 migrations apply dageolgo-db --remote`
    3. Worker 재배포:
       `wrangler deploy`
    4. 최종 확인: 인증 토큰 포함 `GET /api/v1/notifications`가 `200`으로 응답하는지 검증.

### 🚨 문제: 회원가입 `/api/v1/auth/signup` 500 에러 (`table users has no column named role`)
*   **증상**: 배포 환경에서 회원가입 요청 시 즉시 `500` 발생.
*   **원인**: 코드가 `users.role` 컬럼에 INSERT하지만, 기존 D1 스키마에는 해당 컬럼이 없음.
*   **해결 방법**:
    1. `apps/worker-api/migrations/0001_add_users_role.sql` 추가.
    2. 원격 적용:
       `wrangler d1 migrations apply dageolgo-db --remote`
    3. 재검증: `POST /api/v1/auth/signup`가 `201`로 응답하는지 확인.

### 🚨 문제: 게시글/댓글 작성 시 AI 검토 없이 등록되는 것처럼 보임
*   **증상**: AI 백엔드 응답이 실패해도 게시글/댓글이 그대로 등록됨.
*   **원인**: `server.mjs`에서 `requestAiReview` 실패 시 `aiReviewResult`(로컬 키워드 판정)로 fallback 하도록 구현되어 있어 AI 백엔드 장애가 차단 조건이 아니었음.
*   **해결 방법**:
    1. 게시글/댓글 작성 경로에서 fallback 제거.
    2. AI 응답이 없거나 형식이 유효하지 않으면 `503 AI_REVIEW_UNAVAILABLE` 반환하도록 변경.
    3. Worker 재배포 후 검증:
       정상 시 `POST /api/v1/posts`는 `200`,
       AI 백엔드 장애 시 등록 차단(`503`)되는지 확인.

---

## 💡 개발자 팁
*   **DB 스키마 변경 시**: `infra/d1/migrations`의 SQL 파일과 `apps/worker-api/src/db/sqliteAdapter.mjs`를 동시에 업데이트해야 상용 배포 시 에러가 없습니다.
*   **AI 모델 튜닝 시**: `apps/ai-backend/.env`의 `OPENAI_API_KEY`가 유효한지 항상 확인하세요.

---

## 2026-05-16 업데이트

- Worker 인증 흐름 안정화: `POST /api/v1/auth/refresh` 라우트 추가(토큰 갱신 404 해결)
- 알림 API 안정화: D1 어댑터 `notifications` 메서드 보강 + 마이그레이션 반영
- 프로필/통계 호환성: `/api/v1/auth/me` 응답에 `id` 포함, `/api/v1/stats/me` 레거시 경로 호환 추가
- AI 검토 정책 강화: 욕설 외 비난/조롱/낙인성 표현까지 `SOFT_WARN/BLOCK` 범위 확장
- 작성 UX 개선: 경고/차단 시 `수정하기`와 `그래도 올리기(forcePublish)` 선택 가능
- 운영 점검 결과: Railway AI 백엔드는 응답 중이나, OpenAI 키 실사용 여부는 Railway 최신 배포/환경변수 확인이 필요
