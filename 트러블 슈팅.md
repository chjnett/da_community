# 트러블 슈팅

## 1. `POST /api/v1/auth/signup` 500
- 증상: 회원가입 즉시 500
- 원인: D1 `users.role` 컬럼 누락
- 해결:
  - 마이그레이션 추가 후 원격 적용
  - `wrangler d1 migrations apply dageolgo-db --remote`

## 2. `GET /api/v1/notifications` 500
- 증상: 알림 페이지 진입 시 500
- 원인:
  - D1 어댑터에 `listNotifications`/`insertNotification`/`markNotificationAsRead` 누락
  - D1 `notifications` 테이블 미존재
- 해결:
  - 어댑터 메서드 구현
  - notifications 마이그레이션 적용
  - Worker 재배포

## 3. `GET /api/v1/notifications` 401 + `/auth/refresh` 404
- 증상: 세션 만료 후 자동 복구 실패
- 원인: Worker에 `POST /api/v1/auth/refresh` 라우트 없음
- 해결: refresh 라우트 추가 및 재배포

## 4. `/auth/me` API Validation Warning (`id` required)
- 증상: 프론트 스키마 경고
- 원인: `/auth/me` 응답에 `id` 누락
- 해결: `toProfile` 응답에 `id` 포함

## 5. `GET /api/v1/stats/me` 404
- 증상: 프로필 통계 로드 실패
- 원인: 백엔드는 `/auth/stats`만 제공
- 해결: `/stats/me` 레거시 호환 라우트 추가

## 6. AI 검토 우회 체감
- 증상: AI 검토가 약하게 느껴짐
- 원인: 초기 규칙이 욕설 중심
- 해결:
  - 비난/조롱/낙인 표현 패턴 강화
  - 작성 모달에 `수정하기`/`그래도 올리기(forcePublish)` 분기 명확화
